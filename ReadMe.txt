#  This is a simple script that pings an address, checks packet loss to see if the device is online, then sends
an email to the administrator if it is offline, and records status to a log file. 
This script is designed to be used in conjunction with crontab to automate being ran.